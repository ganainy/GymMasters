<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ganainy.dev.gymmasters">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/file_download"
        android:label="@string/app_name"
        android:name=".MyApp"
        android:roundIcon="@mipmap/file_download_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="ganainy.dev.gymmasters.ui.map.MapsActivity"
            android:theme="@style/AppTheme.NoActionBar"/>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_map_key" />

        <activity
            android:name="ganainy.dev.gymmasters.ui.timer.TimerActivity"
            android:label="@string/title_activity_timer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="ganainy.dev.gymmasters.ui.posts.PostsActivity"
            android:configChanges="orientation|keyboardHidden"
            android:screenOrientation="portrait" />
        <activity
            android:name="ganainy.dev.gymmasters.ui.userInfo.UserInfoActivity"
            android:configChanges="orientation|keyboardHidden"
            android:screenOrientation="portrait" />
        <activity
            android:name="ganainy.dev.gymmasters.ui.findUser.FindUsersActivity"
            android:configChanges="orientation|keyboardHidden"
            android:screenOrientation="portrait" />
        <activity
            android:name="ganainy.dev.gymmasters.ui.specificWorkout.SpecificWorkoutActivity"
            android:configChanges="orientation|keyboardHidden"
            android:screenOrientation="portrait" />
        <activity
            android:name="ganainy.dev.gymmasters.ui.specificExercise.SpecificExerciseActivity"
            android:configChanges="orientation|keyboardHidden"
            android:screenOrientation="portrait" />
        <activity
            android:name="ganainy.dev.gymmasters.ui.exercise.ExercisesActivity"
            android:configChanges="orientation|keyboardHidden"
            android:label="@string/title_activity_exercises"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="ganainy.dev.gymmasters.ui.main.MainActivity"
            android:label="@string/title_activity_main"
            />
        <activity
            android:name="ganainy.dev.gymmasters.ui.login.LoginActivity"
            android:configChanges="keyboardHidden"
            android:theme="@style/NoActionBar"
            android:windowSoftInputMode="adjustNothing" />
        <activity
            android:name="ganainy.dev.gymmasters.ui.signup.SignUpActivity"
            android:configChanges="orientation|keyboardHidden"
            android:theme="@style/NoActionBar"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name="ganainy.dev.gymmasters.ui.welcome.WelcomeActivity"
            android:configChanges="orientation|keyboardHidden"
            android:screenOrientation="portrait"
            android:theme="@style/NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


    </application>

</manifest>