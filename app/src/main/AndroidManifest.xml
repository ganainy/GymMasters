<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/dumbbell"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.SpecificWorkoutActivity"></activity>
        <activity android:name=".activities.AddNewExerciseActivity" />
        <activity android:name=".activities.SpecificExerciseActivity" />
        <activity
            android:name=".activities.ExercisesActivity"
            android:label="@string/title_activity_exercises"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activities.LoginActivity"
            android:theme="@style/NoActionBar" />
        <activity
            android:name=".activities.SignUpActivity"
            android:theme="@style/NoActionBar" />
        <activity
            android:name=".activities.WelcomeActivity"
            android:theme="@style/NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>